mixin card(img, title, link="exc-item.html")
    .wrapper.col-sm-12.col-lg-3.col-md-5
        .card
            img(src="./img/excursion-img/"+ img)
            .descriptions
                h3 #{title}
                .info
                    block
                a(href=link).button.
                    Подробнее